'use strict';

describe('', function() {

	var scope;
	var element;
	var $httpBackend;
	var $rootScope,
		$compile;

	beforeEach(module('mms'));
	beforeEach(module('mms.directives'));

	beforeEach(function() {
		inject(function($injector) {

		});
	});

	it('', function() {

	});


});